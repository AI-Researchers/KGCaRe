# LightRAG Package

Install from this directory:

```bash
python -m pip install -e .
python -m pip install -r requirements-offline.txt
```

Run the dataset adapters from:

```text
lightRAG/adapters/
```

See:

```text
lightRAG/adapters/README.md
```
